// Tipos de tipos primitivos: string, number, undefined, null, boolean, symbol.

//Strings:
const nome = "Albino"; //String
const nome1 = "Albino"; //String
const nome2 = "Albino"; //String

//Number's:
const num1 = 10; //Number - subtipo: inteiro (Int)
const num2 = 10.52; //Number - subtipo: número de ponto flutuante ou real (Float Point)

//Undefined e Null:
let nomeAluno; //undefined = Não aponta pra local nenhum na memória = sem valor atribuído a variável.
let sobrenomeAluno = null; // Nulo -> Não aponta para local nenhum na memória. Usa-se para "anular o valor da variável até que fassa-se necessário não mais fazê-lo."
//Diferença entre null e undefined = null tu escolhe quando quer que o valor da variável realmente seja nulo. E o undefined e o próprio JavaScript que decide quando não aponta para nada na memória.

//Boolean:
const boolean1 = true; //Boolean
const boolean2 = false; //Boolean
const aprovado = true; //Boolean
//Valores booleanos servem para criar condicionais.
